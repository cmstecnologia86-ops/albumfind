# Configure these values locally in .env.local and in Vercel Environment Variables.
# Do not commit the real values.
ALBUMFIND_PIN=0000
ALBUMFIND_AUTH_SECRET=replace-with-a-random-secret-of-at-least-24-characters
